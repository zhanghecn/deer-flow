# Backend reads env from ../.env (see backend/langgraph.json).
# Keep a single env source at project root:
#   cp ../.env.example ../.env
#
# This file is optional and only for backend-only local overrides.
