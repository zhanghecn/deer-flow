# Use project root .env as the single source of truth:
#   cp ../.env.example ../.env
#
# This file is optional and only for local overrides while developing gateway-go
# in isolation.
# Example:
# DATABASE_URI=postgresql://postgres:postgres@127.0.0.1:5432/openagents?sslmode=disable
