# 正式构建默认直接走当前访问 origin 下的同源 `/api`。
# 只有在你明确希望浏览器跨域直连其他后端时，才需要填这个变量。
VITE_BACKEND_BASE_URL=

# 正式站点默认不是“纯静态只读模式”。
VITE_STATIC_WEBSITE_ONLY=false

# 正式构建可改为 user，让工具调用默认显示为用户友好的“查找资料”。
VITE_MESSAGE_TRACE_DISPLAY_MODE=debug
