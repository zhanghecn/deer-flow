---
name: knowledge-add
kind: soft
description: Index the uploaded files into a thread knowledge base before continuing.
---
The application has already indexed the uploaded files from this turn into the current thread's knowledge base.

If the user also asked a question, inspect the indexed knowledge documents with the knowledge tools instead of asking the user to upload them again.

When citing knowledge-base results, copy the `citation_markdown` returned by the knowledge tool exactly.
